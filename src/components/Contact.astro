---
import { siteContent } from '../content/site';
const { contact, business } = siteContent;
---

<section id="contact" class="pt-1 md:pt-2 pb-10 md:pb-14">
  <div class="container-edge grid md:grid-cols-2 gap-12 md:gap-16">
    <div class="max-w-md">
      <p class="eyebrow eyebrow-divider">{contact.eyebrow}</p>
      <h2 class="headline headline-section mt-8">{contact.headline}</h2>
      <p class="body-copy mt-6">{contact.subhead}</p>

      <div class="mt-10 space-y-4">
        <div>
          <p class="eyebrow">Phone</p>
          <a
            href={`tel:${business.phone.replace(/[^0-9+]/g, '')}`}
            class="font-body text-lg text-ivory hover:text-brass transition-colors mt-1 inline-block"
          >
            {business.phone}
          </a>
        </div>
        <div>
          <p class="eyebrow">Email</p>
          <a
            href={`mailto:${business.email}`}
            class="font-body text-lg text-ivory hover:text-brass transition-colors mt-1 inline-block"
          >
            {business.email}
          </a>
        </div>
        <div>
          <p class="eyebrow">Service area</p>
          <p class="font-body text-base text-muted mt-1">{business.serviceArea}</p>
        </div>
      </div>
    </div>

    <form
      action={contact.formEndpoint}
      method="POST"
      class="space-y-6"
    >
      <div>
        <label for="name" class="form-label">Name <span class="text-red-500" aria-hidden="true">*</span></label>
        <input id="name" name="Name" type="text" required class="form-input" />
      </div>
      <div class="grid sm:grid-cols-2 gap-6">
        <div>
          <label for="email" class="form-label">Email <span class="text-red-500" aria-hidden="true">*</span></label>
          <input id="email" name="Email" type="email" required class="form-input" />
        </div>
        <div>
          <label for="phone" class="form-label">Phone</label>
          <input id="phone" name="Phone" type="tel" class="form-input" />
        </div>
      </div>
      <div>
        <label for="address" class="form-label">Address</label>
        <input
          id="address"
          name="Address"
          type="text"
          autocomplete="street-address"
          class="form-input"
        />
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-4 gap-6">
        <div class="sm:col-span-2">
          <label for="city" class="form-label">City</label>
          <input
            id="city"
            name="City"
            type="text"
            autocomplete="address-level2"
            class="form-input"
          />
        </div>
        <div>
          <label for="state" class="form-label">State</label>
          <input
            id="state"
            name="State"
            type="text"
            autocomplete="address-level1"
            maxlength="2"
            class="form-input uppercase"
          />
        </div>
        <div>
          <label for="zip" class="form-label">Zip</label>
          <input
            id="zip"
            name="Zip"
            type="text"
            autocomplete="postal-code"
            inputmode="numeric"
            pattern="[0-9]{5}(-[0-9]{4})?"
            maxlength="10"
            class="form-input"
          />
        </div>
      </div>
      <div>
        <label for="project-type" class="form-label">Project type</label>
        <select id="project-type" name="Project Type" class="form-select">
          <option value="">Choose one</option>
          {contact.projectTypes.map((t) => <option value={t}>{t}</option>)}
        </select>
      </div>
      <div>
        <label for="message" class="form-label">Message</label>
        <textarea id="message" name="Message" rows="4" class="form-textarea"></textarea>
      </div>
      <div class="pt-2">
        <button type="submit" class="btn-primary w-full sm:w-auto">
          Submit
        </button>
      </div>
    </form>
  </div>
</section>
